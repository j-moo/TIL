# 시험 키워드
1. RSS 뜻과 정의
2. 신경망
3. 경사하강법
4. 로지스틱 회귀
5. 손실함수

### RSS 뜻과 정의
- 실제 관측값과 예측값의 차이(잔차)를 제곱해 합한 값

### 요약 및 정리
- 선현회귀 개요
  - 지도학습의 가장 기초적인 방법
  - 입력과 출력의 선형 관계를 학습
- 단순선형회귀
  - 모형 : Y = B_0 + B_1X + e
  - 의미 : B_0(절편), B_1(기울기), e(측정오차)
  - 학습방법 : 최소제곱법 (RSS 최소화)
  - 사례 : TV 광고비와 매출 관계
- 다중선형회귀
  - 모형 : B_0 + B_1X_1 + ... + e
  - 해석 : 다른 변수 고정 시, 특정 X_j 가 1 증가 -> Y는 평균적으로 B_j 변화
  - 사례 : TV + Radio + Newspaper 광고비와 매출
- 계수 추정과 예측
  - RSS 최소화를 통해 계수 측정
  - 해 : 최소제곱 => 정규방정식 사용
  - 결과는 평면(hyperplane)으로 표현
- 선형회귀 주의사항
  - 훈련 데이터 기반 계산 -> 검증/테스트셋 필요
  - 다중공선성 문제 발생 가능 (변수 간 높은 상관성)
    - 계수 해석 어려움
    - 분산 증가 -> 불안정한 추정

### 확인문제
1. 다음 질문에 대해 서술하시오
   1. 선형회귀의 기본 목적은 무엇인가?
   2. 단순선형회귀 모형의 수식과 각 게수의 의미는?
   3. 전차(Residual)의 정의와 역할은?
   4. 최소제곱법(Least Squares)의 핵심 아이디어는?
    ```
    정답 및 해설
    1. 입력 변수와 출력 변수 간의 선형 관계를 찾고, 이를 통해 예측 및 해석을 수행하는 것
    2. 모형 : Y = B_0 + B_1X +e
    3. 잔차 = 실제값 - 예측값. 모델이 설명하지 못한 부분으로, 모델 적합도를 판단하는 지표
    4. 잔차제곱합(RSS)를 최소화하는 계수를 찾는 방법으로, 데이터와 회귀선 간의 차이를 최소화하는 원리
    ```

---

### 요약 및 정리
- 분류 문제란?
  - 분류 : 정해진 범주 (카테고리) 중 하나로 지정하는 것
  - 분류 모델의 목표 : 분류 함수 f(X)를 학습하여 입력 X가 속할 범주(또는 그 확률)를 예측
- 분류 문제에서 선형회귀 모델 적용의 한계
  - 이진 분류 문제 : 선형회귀는 예측 값이 확률 범위(0~1)를 벗어나 부적합
  - 다중 분류 문제 : 정수로 코딩 시 인위적 순서를 암시하기에 부적함
- 로지스틱 회귀는 어떻게 작동할까?
  - 로지스틱 함수 모형식은 선형 회귀 모형식과 sigmoid 함수의 결합
  - 우도 : "확률함수가 데이터를 얼마나 잘 설명하는지"를 나타낸 지표
  - 최대 우도 추정 : 우도가 최대가 되는 모수를 찾는 과정

### 확인문제
1. 다음 중 틀린 설명은?
   1. 다중범주 문제에서 선형회귀보다 Logistic Regression 또는 다른 분류 모델이 적절하다.
   2. 선형 회귀 예측 값을 확률로 해석하면 0미만 또는 1초과가 나올 수 있어 확률로서 타당하지 않다.
   3. 기상 상태 예측 모델을 만들 때 정수형 코딩을 진행해야만 적절한 예측이 가능하다.
   4. 범주형 변수는 순서가 없는 집합에서 값을 가진다.
    ```
    정답 및 해설
    정답 : 3
    해설 : 범주형 변수를 정수 코딩했을 때, 인위적 순서를 암시하고 1<->2 와 2<->3의 거리 동일성까지 암묵적으로 가정하게 됨.
    ```

2. 다음 중 로지스틱 함수의 출력으로 옳은 것은?
   1. [0, 1]
   2. (0, 1)
    ```
    정답 및 해설
    정답 : B
    해설 : 로지스틱 회귀의 출력은 어떤 X, B에 대해서도 (0, 1)가 보장됩니다. A가 답이 아닌 이유는 끝점 0과 1을 포함하기 때문입니다. 로지스틱 회귀의 출력 중 0과 1은 극한으로만 접근합니다.
    ```

3. 다음 문장이 옳은가? (o/x)
   - 로지스틱 회귀에서 "현재 확률 함수가 데이터를 얼마나 잘 설명하는지"를 나타낸 지표로 사용하는 것은 평균 제곱 오차이다.
    ```
    정답 및 해설
    정답 : x
    해설 : 로지스틱 회귀에서 모수(가중치)를 추정할 때 사용하는 표준 지표는 평균제곱오차가 아니라, 우도를 최대화하는 방법이다.

---

### 손실함수
- 학습 데이터 셋 : input/output의 i개 쌍
- 손실함수(Loss function): 모델이 얼마나 잘못 예측하는지를 측정하는 함수
  - 값이 더 작을수록 모델이 더 정확하게 학습되었다는 의미

### 경사 하강법
- 경사 하강 알고리즘
  - 경사 하강법은 손실 함수를 최소화하기 위해 파라미터를 반복적으로 갱신하는 알고리즘
  - 기울기 계산 : 손실 함수를 파라미터에 대해 편미분 진행

### 요약 및 정리
- 경사 하강법 (Gradient Descent)
  - step 1 : 손실함수의 기울기 (미분값) 계산
  - step 2 : 기울기의 반대 방향으로 파라미터 업데이트
  - 학습률 : 한 번에 이동한는 크기 결정
- Convex 문제와 최적화
  - 2차 미분이 항상 양수 -> 아래로 볼록한 형태
  - 국소 최솟값 = 전역 최솟값 -> 안정적인 학습 가능
- 확률적 경사 하강법 (SGD)
  - 전체가 아닌 일부(미니배치)데이터로 기울기 계산 -> 계산 비용 감소
  - 매 업데이트마다 노이즈가 섞여 국소 최솟값에서 벗어나기 유리